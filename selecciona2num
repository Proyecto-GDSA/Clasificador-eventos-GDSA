function salida=selecciona2num(v,n)
% se le añade el vector de ID i saca todas las que acaben en el numero n

L=length(v);
f=1;

for i=2:L-1
    S=v(i,1);
    S=char(S);
    N=length(S);
    if strcmp(n(2), S(N)) & strcmp(n(1),S(N-1))
        
       if f==1
           f=2;
           salida=v(i,:);
       else
           salida=[salida;v(i,:)];
       end
    end
end
end
           
